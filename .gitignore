# Dependencias
node_modules/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Entorno
.env*
!.env.example

# Sistema y editores
.DS_Store
Thumbs.db
*.log

# Ajustes locales de las herramientas de agente. El contexto compartido vive en
# AGENTS.md y .agents/, que si se versionan. Esto guarda permisos y rutas
# absolutas de cada maquina, que no le sirven a nadie mas.
.claude/

# Nota: uis/website/styles.css SI se versiona. Lo genera el CLI de Tailwind,
# pero el sitio es estatico y debe funcionar sin pasos de build al clonarlo.

# Salida de compilacion de TypeScript
packages/talent-core/dist/

# Herramientas locales de QA de las interfaces (no forman parte del entregable)
uis/*/_*.html
